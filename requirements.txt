# Core framework
Django==6.0.8

# Image handling (needed for Book.cover_image / ImageField)
pillow==12.3.0

# Excel export (staff reports: most-borrowed, fines, active readers)
openpyxl==3.1.5

# PDF generation (library card)
reportlab==5.0.1

# Form/search helpers (installed and ready to use — see README "Optional cleanup")
django-crispy-forms==2.7
crispy-bootstrap5==2026.9
django-filter==26.1

# Loads DJANGO_SECRET_KEY etc. from a local .env file
python-dotenv==1.2.3
